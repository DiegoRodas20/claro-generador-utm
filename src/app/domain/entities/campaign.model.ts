export interface Campaign {
    id: number,
    name: string
}

export interface TypeCampaign {
    id: number,
    name: string
}

export interface SourceCampaign {
    id: number,
    name: string
}